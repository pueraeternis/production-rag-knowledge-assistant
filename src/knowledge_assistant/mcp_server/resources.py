"""
Deferred MCP resource URIs for repository browsing (Plan 10b / Plan 12+).

Tier 1 knowledge access does not require MCP resources. Future resources may include:

* ``knowledge://documents`` — document catalog
* ``knowledge://documents/{document_path}`` — full document content
* ``knowledge://stats`` — collection statistics

No resource handlers are implemented in Plan 10.
"""
